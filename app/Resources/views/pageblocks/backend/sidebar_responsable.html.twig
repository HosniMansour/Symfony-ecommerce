<div class="page-sidebar" id="main-menu">
    <div class="page-sidebar-wrapper scrollbar-dynamic" id="main-menu-wrapper">
        <div class="user-info-wrapper">
            {%
                set img = 'img/default-avatar.png'
            %}
            {% if app.user.image is defined %}
                {% if app.user.image is not null %}
                    {% set img = app.user.image.image %}
                {% endif %}
            {% endif %}
            <div class="profile-wrapper"> <img src="{{ asset(img) }}"  alt="" data-src="{{ asset(img) }}" data-src-retina="{{ asset(img) }}" width="69" height="69" /> </div>
            <div class="user-info">
                <div class="greeting">Bonjour</div>
                <div class="username"> {% if app.user.nom is not null %} {{ app.user.nom }} <br/><span class="semi-bold"> {{ app.user.prenom }} </span> {% else %} {{ app.user.username }} {% endif %} </div>
                <br/>
            </div>
        </div>
        <p class="menu-title">BROWSE <span class="pull-right"><a href="javascript:;" onclick="window.location.reload(false); "><i class="fa fa-refresh"></i></a></span></p>
            <ul style="margin-bottom: 100px;">
                <li class="start"> <a href="{{ path ('web_admin_homepage') }}"> <i class="fa fa-home"></i> <span class="title">Accueil</span>  </a> </li>
                <li class=""> <a href="javascript:;"> <i class="fa fa-shopping-cart"></i> <span class="title">Produits</span> <span class="arrow "></span> </a>
                    <ul class="sub-menu">
                        <li > <a href="{{ path ('produit_new') }}">Ajouter Produit</a> </li>
                        <li > <a href="{{ path ('produit_index') }}">Toutes Les Produits</a> </li>
                    </ul>
                </li>
                <li class=""> <a href="javascript:;"> <i class="fa fa-folder-open-o"></i> <span class="title">Categories</span> <span class="arrow "></span> </a>
                    <ul class="sub-menu">
                        <li > <a href="{{ path ('categorie_new')}}">Ajouter Categorie</a> </li>
                        <li > <a href="{{ path ('categorie_index') }}">Toutes Les Categories</a> </li>
                    </ul>
                </li>
                <li class=""> <a href="{{ path ('media_index')}}"> <i class="fa fa-picture-o"></i> <span class="title">Media</span> </a> </li>
                <li class=""> <a href="{{ path('fos_user_security_logout') }}"> <i class="fa fa-sign-out"></i> <span class="title">Déconnexion</span> </a> </li>
            </ul>
        <div class="clearfix"></div>
    </div>
</div>
